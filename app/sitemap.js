// SEO sitemap — lists all public routes for search engines.
// TODO: Generate entries dynamically from config/classes.js and DB-backed class slugs.
export default function sitemap() {
  return [];
}
