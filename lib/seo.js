// Metadata + JSON-LD helpers (LocalBusiness, Course schema) for SEO.
// TODO: Implement buildMetadata(page) and localBusinessJsonLd() using config/site.js.
export function buildMetadata() {
  return {};
}
